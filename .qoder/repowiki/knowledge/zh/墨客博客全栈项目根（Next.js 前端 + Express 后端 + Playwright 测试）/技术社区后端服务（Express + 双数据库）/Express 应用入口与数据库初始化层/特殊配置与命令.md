首次启动时若 `posts` 表为空会自动调用 `seed()` 填充初始数据；`syncSequences` 会遍历 posts/users/categories/comments/post_tags/columns/user_follows/user_points/post_favorites/column_posts 九张表重建自增序列，确保手动 INSERT 后 `nextval` 不会回退。