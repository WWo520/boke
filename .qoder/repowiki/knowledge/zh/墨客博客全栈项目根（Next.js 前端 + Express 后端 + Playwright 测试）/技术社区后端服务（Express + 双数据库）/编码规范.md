- 所有 REST 接口统一以 `/api/<资源名>` 前缀组织，每个资源一个 `routes/*.js` 文件并通过 `app.use` 挂载。
- 受保护路由一律先挂 `authMiddleware`，管理员操作再追加 `adminMiddleware`，禁止在路由内部重复校验 JWT。
- 数据库访问只通过 `queryOne/queryAll/runSql` 三个封装函数，SQL 参数使用 `$1/$2` 占位符防止注入。
- 错误响应统一采用 `{ error: { code, message } }` 结构，HTTP 状态码由具体 handler 设置。